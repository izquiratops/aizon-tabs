name: Testing

on:
  push:
    branches:
      - development

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4.1.2

    - name: Install dependencies
      run: npm install

    - name: Run Tests
      run: npm run test
